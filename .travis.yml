language: c++

matrix:
  include:
    - os: linux
      compiler:
        - gcc
        - clang
      addons:
        apt:
          update: true
          sources:
            - george-edison55-precise-backports
          packages:
            - cmake-data
            - cmake

before_install:
  - eval "${MATRIX_EVAL}"

script: scripts/ci/test.sh